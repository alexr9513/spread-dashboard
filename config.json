{
  "_README": [
    "MINIMAL CONFIG — only the essentials.",
    "Everything else (filters, table columns, axes) is configured",
    "directly in the dashboard browser UI via the ⚙ SETTINGS button.",
    "",
    "  1. Set 'input_file' to your data file (.parquet, .csv, .xlsx)",
    "  2. Map the 4 essential columns below to YOUR column names",
    "  3. Run:  python dashboard.py",
    "  4. Serve: python -m http.server 8000",
    "  5. Open the dashboard and click ⚙ SETTINGS to configure filters"
  ],

  "input_file": "data\\Base_EUR_HY_v3.parquet",

  "columns_essential": {
    "bond_id": {
      "your_column": "Cusip",
      "dashboard_key": "isin",
      "description": "Unique bond identifier"
    },
    "spread": {
      "your_column": "OAS vs Govt",
      "dashboard_key": "oas",
      "description": "Bond spread in basis points"
    },
    "maturity_date": {
      "your_column": "Maturity",
      "dashboard_key": "maturity",
      "description": "Bond maturity date"
    },
    "snapshot_date": {
      "your_column": "Date",
      "dashboard_key": "date",
      "description": "Date of the data snapshot"
    }
  }
}